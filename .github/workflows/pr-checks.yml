name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: 📦 Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔍 Lint
        run: pnpm run lint

      - name: ✅ Type check
        run: pnpm run typecheck

      - name: 🧪 Run unit tests with coverage
        run: pnpm exec vitest run --coverage --ci

      - name: 🎭 Install Playwright
        run: pnpm exec playwright install --with-deps

      - name: 🌐 Run E2E tests
        run: pnpm run test:e2e -- --reporter=list --ci

      - name: 📦 Build
        run: pnpm run build
